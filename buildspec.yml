version: 0.2
phases:
  install:
    commands:
      - echo "Installing dependencies (if any)"
  build:
    commands:
      - echo "Build stage - nothing to compile, just packaging"
artifacts:
  files:
    - index.html
    - appspec.yml
    - scripts/*
